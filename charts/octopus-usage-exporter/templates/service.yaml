apiVersion: v1
kind: Service
metadata:
  name: {{ include "octopus-usage-exporter.fullname" . }}
  labels:
    {{- include "octopus-usage-exporter.labels" . | nindent 4 }}
  {{- with .Values.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "octopus-usage-exporter.selectorLabels" . | nindent 4 }}
